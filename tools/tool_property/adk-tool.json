{
  "id": "property",
  "title": "车辆属性（CarProperty）生成与导入",
  "entry": "main.py",
  "order": 10,
  "interactive_project_pick": {
    "config": "config.json",
    "projects_key": "projects"
  },
  "hint": "无参时执行完整流水线（scan + fetch + generate + deploy + snapshot）。\n若上一步已在交互菜单中选择项目，可直接回车对该项跑完整流水线；也可输入下表命令片段（已选项目时多数可省略行末项目名）。\n下列「参数选择」为通用写法；「参数示例」演示带项目名；每条下方为能力说明。",
  "choices": [
    {
      "arg": "list",
      "desc": "列出所有已配置项目：飞书表链接、input 最新 Excel、各变体 deploy 路径与分支等状态，不执行下载或生成。"
    },
    {
      "arg": "scan",
      "desc": "将飞书最新表格内容与本地已有版本逐单元格对比，列出所有差异项（增/删/改颜色高亮），供决策是否需要更新。不下载文件。"
    },
    {
      "arg": "fetch",
      "desc": "仅从飞书拉取各项目在线属性表到本地 input/（按项目目录存放），不生成代码、不部署、不创建版本快照。"
    },
    {
      "arg": "generate",
      "desc": "仅根据 input/ 中已有 Excel 生成 C/C++ 头文件等到 output/，不拉表、不部署（若未先 fetch 则可能无新表）。"
    },
    {
      "arg": "deploy",
      "desc": "仅将 output/ 中已生成文件增量同步到各项目配置的 Git 仓库目标路径，不拉表、不重新生成。"
    },
    {
      "arg": "snapshot",
      "desc": "仅为飞书在线表格创建命名版本快照，不下载、不生成、不部署。"
    },
    {
      "arg": "scan fetch generate deploy snapshot",
      "desc": "显式串联五步完整流水线：扫描 → 拉表 → 生成 → 部署 → 快照；与无参默认行为一致。"
    }
  ],
  "examples": [
    {
      "arg": "BAIC",
      "desc": "仅针对 BAIC 项目执行完整流水线（scan + fetch + generate + deploy + snapshot），其它项目不处理。"
    },
    {
      "arg": "scan BAIC",
      "desc": "仅对 BAIC 扫描飞书与本地差异，不下载、不生成、不部署。"
    },
    {
      "arg": "fetch generate BAIC",
      "desc": "仅对 BAIC 执行拉表与生成，不执行 deploy，适合先本地检查生成结果再手动部署。"
    }
  ]
}
